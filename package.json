{
  "name": "pr-gauntlet-gl",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  },
  "scripts": {
    "test:score": "playwright test scoring/ --config scoring/playwright.config.js"
  }
}
